# 悬浮量角器 Android App

这是一个可以悬浮在其他应用上方的量角器示例：

- 一条基准边固定为水平线。
- 另一条可拖拽旋转，实时显示夹角（0° ~ 180°）。
- 整个量角器浮窗可拖动位置。
- 提供关闭按钮，或在主页面点击“关闭悬浮量角器”停止服务。

## 运行方式

1. 用 Android Studio 打开项目。
2. 同步 Gradle 并运行 `app` 模块。
3. 打开应用后授予“显示在其他应用上层”权限。
4. 点击“启动悬浮量角器”。

> 该实现使用 `TYPE_APPLICATION_OVERLAY` + 前台服务实现跨应用悬浮。
